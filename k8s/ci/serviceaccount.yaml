# Identity for the deploy-poller CronJob and the kaniko build Jobs it
# spawns. Scoped to the transigen namespace only — this is not a
# cluster-admin account.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: deploy-poller
  namespace: transigen
