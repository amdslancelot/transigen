# Stable in-cluster address for the app; the Ingress routes external traffic
# here.
apiVersion: v1
kind: Service
metadata:
  name: transigen
spec:
  selector:
    app: transigen
  ports:
    - name: http
      port: 80
      targetPort: 3000
